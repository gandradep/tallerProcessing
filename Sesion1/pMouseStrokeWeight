//ejemplo de Libor Learning Processing - Daniel Shiffman
void setup() {
  size(640, 360);
  background(102);
}

void draw() {
  stroke(255);
  int x = abs(mouseX-pmouseX);
  strokeWeight(x);
  println(x);
 
    line(mouseX, mouseY, pmouseX, pmouseY);
  
}
